import 'package:flutter/material.dart';
import 'package:get/get.dart';

/* Konstan yang bersifat General (umum), sering dibutuhkan oleh seluruh Modul */
class ConstGeneral {
  static final mediaSize = Get.size;
  static final mediaHeight = Get.size.height;
  static final mediaWidth = Get.size.width;

  static const Color primaryBgColor = Color(0xFF08AEF0);

  static const isNewUIXplorin = false;
}
